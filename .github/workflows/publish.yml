name: Publish Package from Git Tag

on:
  push:
    tags:
      - 'v*'  # Trigger only on tags like v1.0.0, v1.0.1

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      # 1️⃣ Checkout the repo
      - uses: actions/checkout@v3

      # 2️⃣ Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org/'

      # 3️⃣ Authenticate to npm
      - name: Authenticate to npm
        run: npm config set //registry.npmjs.org/:_authToken=${{ secrets.NPM_TOKEN }}

      # 4️⃣ Install dependencies
      - name: Install dependencies
        run: npm ci

      # 5️⃣ Build the package (optional)
      - name: Build package
        run: npm run build

      # 6️⃣ Set package.json version from Git tag
      - name: Set version from Git tag
        run: |
          # Get the tag name, remove leading "v"
          VERSION=${GITHUB_REF##refs/tags/v}
          echo "Using version $VERSION from Git tag"
          npm version $VERSION --no-git-tag-version

      # 7️⃣ Publish to npm
      - name: Publish package
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
